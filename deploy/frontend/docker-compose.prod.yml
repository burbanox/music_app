version: "3.9"

services:
  frontend:
    build:
      context: ../../frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: http://IP_PUBLICA_BACKEND:8000
    container_name: chinook_frontend
    restart: always
    ports:
      - "80:80"