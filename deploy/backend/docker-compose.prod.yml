version: "3.9"

services:
  backend:
    build:
      context: ../../backend
    container_name: chinook_backend
    restart: always
    env_file:
      - ../../backend/.env
    ports:
      - "8000:8000"