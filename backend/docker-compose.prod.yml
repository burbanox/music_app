version: "3.9"

services:
  backend:
    build:
      context: /home/ec2-user/app/backend
    container_name: chinook_backend
    restart: always
    env_file:
      - /home/ec2-user/app/backend/.env
    ports:
      - "8000:8000"